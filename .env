# Vite environment variables
# Change this URL to your backend API base
# If your backend exposes endpoints like http://localhost:8080/api/login,
# keep the /api suffix here so FE can call api.post('/login') -> .../api/login
VITE_API_BASE_URL=http://localhost:8000/api
